localhost:2000 {

    @api path /api/*

    rate_limit @api {
        zone api_zone {
            events 30
            window 10s
        }
        log_key
    }

    log @api {
        output file caddy.log
        level DEBUG
    }

    reverse_proxy localhost:3000
}

